# 1. Choose language and target JDKs for parallel builds.
language: java
jdk:
  - oraclejdk8

# 2. Setup cache directory for Maven.
cache:
  directories:
  - $HOME/.m2

# 3. Turn off notifications.
notifications:
  email: false

